模板题目：[P5960 【模板】差分约束算法](https://www.luogu.com.cn/problem/P5960)

使用：

```cpp
#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
typedef long long LL;
typedef unsigned long long uLL;
const int MAXN = 5e3 + 100;
const int MAXM = 5e3 + 100 + MAXN;
const int INF = INT_MAX;

int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 1, u, v, w; i <= m; i++) {
        cin >> u >> v >> w;
        g.addedge(v, u, w);
    }
    for (int i = 1; i <= n; i++)
        g.addedge(0, i, 0);
    if (!SPFA::SPFA(n, 0))
        puts("NO");
    else
        for (int i = 1; i <= n; i++)
            cout << SPFA::dis[i] << ' ';
    return 0;
}
```